// Placeholder for Phase 6+ host-side unit tests (Lz4, JSON payload shape,
// queue file semantics). Confirms the test/ build wiring works.
#include <cstdio>

int main() {
    std::printf("smoke_test ok\n");
    return 0;
}
